---
"@quartz-community/footer": minor
---

Add `target` and `rel` options to control the `target`/`rel` attributes applied to every footer link (e.g. `target: "_blank"`, `rel: "noopener noreferrer"` to open links in a new tab). Both are omitted by default, so existing configurations are unaffected.
